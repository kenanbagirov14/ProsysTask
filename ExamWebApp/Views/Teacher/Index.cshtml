@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model ICollection<TeacherIndexDTO>
<div class="text-center">
     <a class="btn btn-success btn-sm" asp-area="" asp-controller="Teacher" asp-action="Create">Create</a>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">First Name</th>
                <th scope="col">Last Name</th>
                <th scope="col">#</th>
            </tr>
        </thead>
        <tbody>
            @{
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>
                           <a class="btn btn-danger btn-sm" asp-area="" asp-controller="Teacher" asp-action="Delete" asp-route-id="@item.Id">Delete</a> 
                           <a class="btn btn-info btn-sm" asp-area="" asp-controller="Teacher" asp-action="Update" asp-route-id="@item.Id">Update</a> 
                        </td>
                    </tr>
                }
            }

        </tbody>
    </table>
</div>
